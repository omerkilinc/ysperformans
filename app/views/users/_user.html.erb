<%= content_tag :tr, id: dom_id(user) do %>
  <td><%= user.try(:id) %></td>
  <td><%= user.try(:name) %></td>
  <td><%= user.try(:username) %></td>
  <td class="text-end">
    <div class="btn-group" role="group" aria-label="Basic outlined example">
      <%= link_to "<i class='bi bi-eye'></i>".html_safe, user_path(user),
                  class: "btn btn-outline-secondary",
                  format: :turbo_stream,
                  data: { turbo_frame: "modal1" }
      %>
      <%= link_to "<i class='bi bi-pencil'></i>".html_safe, edit_user_path(user),
                  class: "btn btn-outline-secondary",
                  format: :turbo_stream,
                  data: { turbo_frame: "modal1" }
      %>
      <%= link_to "<i class='bi bi-trash'></i>".html_safe, user_path(user),
                  class: "btn btn-outline-secondary",
                  data: { turbo_method: :delete, turbo_confirm: "#{user.try(:name)} will be deleted !!!" }
      %>
    </div>
  </td>
<% end %>
