<div class="d-grid gap-3">

  <div class="row">
    <div class="col">
      <div class="card">
        <div class="card-body d-flex justify-content-between">
          <span class="fw-bold fs-2">Is Surecleri</span>

          <a href="<%= new_business_process_path %>"
             class="btn btn-outline-success mt-2"
             data-turbo-frame="modal1">
            <i class='bi bi-plus-lg'></i> Yeni Is Sureci
          </a>

        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <div class="card">
        <div class="card-body">

          <%= search_form_for @search, url: business_processes_path, method: :get do |f| %>
            <table class="table table-striped table-hover table-responsive">
              <thead>
              <tr>
                <th><%= sort_link @search, :id, 'Id', {}, { remote: true, method: :get } %></th>
                <th><%= sort_link @search, :name, "Adi", {}, { remote: true, method: :get } %></th>
                <th><%= sort_link @search, :description, "Aciklama", {}, { remote: true, method: :get } %></th>
                <th class="text-end">Aksiyonlar</th>
              </tr>
              </thead>
              <tbody id="business_processes">
              <%= render @records %>
              </tbody>
            </table>
            <%== render partial: "shared/pagy_nav", locals: { pagy: @pagy } %>
          <% end %>

        </div>
      </div>
    </div>
  </div>

</div>
